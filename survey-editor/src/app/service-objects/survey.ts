export class Survey {
  surveyId: number;
  name: string;
  surveyDefinition: string;
  lastInsertUpdateTS: Date;
  lastInsertUpdateBy: string;
}
export class SurveyData {
  surveyId: number;
  name: string;
  surveyDefinition: string;
  lastInsertUpdateTS: Date;
  lastInsertUpdateBy: string;
}
